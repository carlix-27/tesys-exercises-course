// Consigna
// Crear un array de 5 enteros, asignar valores manualmente y mostrarlos.

#include <stdio.h>

int main() {
    int numeros[5] = {10, 20, 30, 40, 50};
    int i;

    for (i = 0; i < 5; i++) {
        printf("%d\n", numeros[i]);
    }

    return 0;
}
